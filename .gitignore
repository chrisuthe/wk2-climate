# Gradle / Android
.gradle/
build/
local.properties
*.apk
*.aab
*.ap_
*.dex
# Android Studio's profiler output, at the project root only — an
# unanchored `captures/` also swallows docs/captures/, where the written
# vehicle-session evidence lives.
/captures/
.externalNativeBuild/
.cxx/

# Keystores — never commit signing material
*.jks
*.keystore

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store
Thumbs.db

# SDD workspace: per-plan scratch (ledger, briefs, review packages)
.superpowers/
